# API 接口返回数据规范文档

## 1. 概述

本文档定义了 API 接口的统一返回数据格式规范，所有接口必须严格遵循此规范，确保前端能够统一处理响应。

---

## 2. 通用响应结构

### 2.1 基本格式

所有 API 接口返回的 JSON 数据必须包含以下五个字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | int | 是 | 业务状态码，用于标识请求处理结果 |
| `msg` | string | 是 | 状态描述信息，用于前端提示或调试 |
| `data` | mixed | 是 | 响应数据体，无数据时返回 `null` |
| `status_code` | int | 是 | HTTP 状态码，与 HTTP Response Status Code 保持一致 |
| `success` | bool | 是 | 请求是否成功，`true` 表示成功，`false` 表示失败 |

### 2.2 示例

#### 成功响应

```json
{
  "code": 0,
  "msg": "操作成功",
  "data": {
    "id": 1001,
    "name": "张三"
  },
  "status_code": 200,
  "success": true
}
```

#### 列表数据响应

```json
{
  "code": 0,
  "msg": "查询成功",
  "data": {
    "list": [
      { "id": 1, "name": "项目A" },
      { "id": 2, "name": "项目B" }
    ],
    "total": 100,
    "page": 1,
    "page_size": 20
  },
  "status_code": 200,
  "success": true
}
```

#### 失败响应

```json
{
  "code": 10401,
  "msg": "认证失败，请登录后再试",
  "data": null,
  "status_code": 401,
  "success": false
}
```

---

## 3. 业务状态码（code）规范

### 3.1 编码规则

状态码采用 **5 位数字**，结构如下：

```
[HTTP状态码首数字] + [模块编号 2位] + [具体错误编号 2位]
```

- **第 1 位**：对应 HTTP 状态码大类
- **第 2-3 位**：模块编号（00-99）
- **第 4-5 位**：具体错误编号（00-99）

### 3.2 状态码分类

#### 成功类（code 以 0 或 1 开头）

| 状态码 | 说明 | HTTP status_code |
|--------|------|------------------|
| `0` | 操作成功 | 200 |
| `10000` | 创建成功 | 201 |
| `10001` | 已接受处理（异步） | 202 |

#### 客户端错误类（code 以 4 开头）

| 状态码 | 说明 | HTTP status_code |
|--------|------|------------------|
| `10400` | 请求参数错误 | 400 |
| `10401` | 认证失败，请登录后再试 | 401 |
| `10402` | 权限不足，无权访问 | 403 |
| `10404` | 资源不存在 | 404 |
| `10405` | 请求方法不允许 | 405 |
| `10409` | 资源冲突（如重复创建） | 409 |
| `10422` | 请求参数校验失败 | 422 |
| `10429` | 请求过于频繁，请稍后再试 | 429 |

#### 服务端错误类（code 以 5 开头）

| 状态码 | 说明 | HTTP status_code |
|--------|------|------------------|
| `10500` | 服务器内部错误 | 500 |
| `10501` | 功能未实现 | 501 |
| `10502` | 网关错误 | 502 |
| `10503` | 服务暂不可用 | 503 |
| `10504` | 网关超时 | 504 |

#### 业务自定义错误

| 状态码 | 说明 | HTTP status_code |
|--------|------|------------------|
| `20001` | 账号已被禁用 | 200 |
| `20002` | 验证码错误 | 200 |
| `20003` | 密码错误 | 200 |
| `20004` | 用户不存在 | 200 |
| `30001` | 订单已过期 | 200 |
| `30002` | 库存不足 | 200 |
| `30003` | 支付失败 | 200 |

---

## 4. HTTP 状态码（status_code）对照

| status_code | 含义 | 适用场景 |
|-------------|------|----------|
| `200` | OK | 请求成功（包括业务逻辑错误） |
| `201` | Created | 资源创建成功 |
| `400` | Bad Request | 请求格式错误 |
| `401` | Unauthorized | 未认证 |
| `403` | Forbidden | 无权限 |
| `404` | Not Found | 资源不存在 |
| `409` | Conflict | 资源冲突 |
| `422` | Unprocessable Entity | 参数校验失败 |
| `429` | Too Many Requests | 请求频率限制 |
| `500` | Internal Server Error | 服务器内部错误 |
| `502` | Bad Gateway | 网关错误 |
| `503` | Service Unavailable | 服务不可用 |

---

## 5. data 字段规范

### 5.1 基本规则

- **单条数据**：直接返回对象
- **列表数据**：返回包含 `list`、`total`、`page`、`page_size` 的对象
- **无数据**：返回 `null`（禁止返回空字符串 `""` 或空对象 `{}`）
- **布尔结果**：使用对象包装，如 `{ "result": true }`

### 5.2 分页数据格式

```json
{
  "code": 0,
  "msg": "查询成功",
  "data": {
    "list": [],
    "total": 0,
    "page": 1,
    "page_size": 20,
    "total_pages": 0
  },
  "status_code": 200,
  "success": true
}
```

---

## 6. 前端处理建议

### 6.1 统一拦截器示例（JavaScript）

```javascript
// 响应拦截器
axios.interceptors.response.use(
  (response) => {
    const { code, msg, data, success } = response.data;

    if (!success) {
      // 统一错误提示
      showToast(msg || '请求失败');

      // 认证失败处理
      if (code === 10401) {
        redirectToLogin();
      }

      return Promise.reject(new Error(msg));
    }

    return data;
  },
  (error) => {
    // 网络错误处理
    showToast('网络异常，请稍后再试');
    return Promise.reject(error);
  }
);
```

### 6.2 处理流程

```
请求发起
  ├── 网络层判断
  │   ├── 网络异常 → 提示"网络异常" → 结束
  │   └── 正常响应 → 进入业务层判断
  │
  └── 业务层判断 (success)
      ├── success = true  → 正常处理 data
      └── success = false → 根据 code 处理
          ├── 10401 → 跳转登录页
          ├── 10402 → 提示"无权限"
          └── 其他   → 提示 msg 内容
```

---

## 7. 注意事项

1. **`code` 必须唯一**：每个错误场景对应唯一的业务状态码
2. **`msg` 需对用户友好**：避免暴露技术细节，如数据库错误信息
3. **`data` 不能为空字符串**：无数据时统一返回 `null`
4. **`status_code` 与 HTTP 响应码一致**：确保网关和监控系统能正确识别
5. **日志记录**：服务端需记录完整的错误堆栈，`msg` 仅返回用户可读信息
6. **版本兼容**：新增字段时采用向后兼容方式，不要删除或修改已有字段含义

---

## 8. 版本记录

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-06-08 | 初始版本 | - |
